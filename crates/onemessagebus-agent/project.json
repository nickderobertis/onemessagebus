{
  "name": "onemessagebus-agent",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "crates/onemessagebus-agent/src",
  "tags": ["scope:library", "lang:rust", "type:profile"],
  "implicitDependencies": ["onemessagebus"],
  "targets": {
    "bootstrap": { "command": "just _crate-bootstrap" },
    "format": { "command": "just _crate-format onemessagebus-agent" },
    "format-check": { "command": "just _crate-fmt-check onemessagebus-agent" },
    "lint": { "command": "just _crate-lint onemessagebus-agent" },
    "build": { "command": "just _crate-build onemessagebus-agent" },
    "test": { "command": "just _crate-test onemessagebus-agent" },
    "doc": { "command": "just _crate-doc onemessagebus-agent" },
    "check": {
      "executor": "nx:noop",
      "dependsOn": ["format-check", "lint", "test", "doc"]
    }
  }
}
