/target
# The Nx orchestrator's own install and computation cache. `scripts/nx`
# re-creates node_modules from the committed lockfile whenever it is missing.
/node_modules
/.nx
# Preserved command output (scripts/preserved-log.sh). Owner-only, truncated per
# run, and never a source: it is the evidence a captured run leaves behind.
/.logs
# Assembled by scripts/npm-build.mjs and maturin; the committed sources are
# npm/onemessagebus-cli/ and pyproject.toml.
/npm/dist
/dist
# The language SDK packages' own installs, builds and test caches; each package's
# committed sources, lockfile and generated contract are what is tracked.
/npm/onemessagebus-sdk/node_modules
/npm/onemessagebus-sdk/dist
/npm/onemessagebus-sdk/coverage
/python/onemessagebus-sdk/.venv
/python/onemessagebus-sdk/dist
__pycache__/
.pytest_cache/
.ruff_cache/
.coverage
# Coverage instrumentation writes these wherever an instrumented binary happens
# to run. They are raw counter dumps, never sources.
*.profraw
# Composed once during buildout and deliberately not committed (see AGENTS.md and
# the create-repo skill): the one-time llmlint structural tier, and the plan the
# composer emitted.
llmlint.buildout.yml
REPO_PLAN.md
# Local sync bookkeeping for gh-secrets (per-machine; the manifest itself is tracked).
.gh-secrets-state.json
