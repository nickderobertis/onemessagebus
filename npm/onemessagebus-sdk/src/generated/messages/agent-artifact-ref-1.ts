// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `bun run generate`.
import { z } from "zod";
import { anyOf, oneOf } from "../runtime.js";
import { registeredMessage } from "../../message.js";

/**
 * Evidence too large for a payload: stored by the producing library and
 * referenced by id, to be read back through that library.
 */
export interface AgentArtifactRefV1 {
  /**
   * Identifier, unique within the producing library's store.
   */
  id: string;
  /**
   * What the artifact is — a gate log, a check log, a transcript, a report.
   */
  kind: string;
  /**
   * Size of the stored artifact.
   */
  bytes: number;
}

export const AgentArtifactRefV1Schema = z.strictObject({
  id: z.string(),
  kind: z.string(),
  bytes: z.int().gte(0),
}) as unknown as z.ZodType<AgentArtifactRefV1>;

/** `agent.artifact-ref@1`, as the Rust registry holds it. */
export const AgentArtifactRefV1 = registeredMessage(
  "agent.artifact-ref@1",
  AgentArtifactRefV1Schema,
  {
    type: "object",
    additionalProperties: false,
    properties: {
      id: {
        type: "string",
        description: "Identifier, unique within the producing library's store.",
      },
      kind: {
        type: "string",
        description: "What the artifact is — a gate log, a check log, a transcript, a report.",
      },
      bytes: {
        type: "integer",
        format: "uint64",
        minimum: 0,
        description: "Size of the stored artifact.",
      },
    },
    required: ["id", "kind", "bytes"],
    description:
      "Evidence too large for a payload: stored by the producing library and\nreferenced by id, to be read back through that library.",
    title: "ArtifactRef",
    $schema: "https://json-schema.org/draft/2020-12/schema",
  },
);
