// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `just node-sdk-generate`.
import { z } from "zod";
import { anyOf, contract, oneOf } from "../runtime.js";
import { registeredMessage } from "../../message.js";

/**
 * One command envelope as `commands.jsonl` holds it.
 */
export interface AgentQueuedCommandsV1 {
  /**
   * The number of envelopes before it.
   */
  id: number;
  /**
   * Who submitted it.
   */
  author?: string | undefined;
  /**
   * The commands, each an object naming its `op`.
   */
  commands: {
    [k: string]: unknown;
  }[];
  [k: string]: unknown;
}

const $Author: z.ZodType = z.string();

export const AgentQueuedCommandsV1Schema = contract<AgentQueuedCommandsV1>(
  z.looseObject({
    id: z.int().gte(0),
    author: z.lazy(() => $Author).optional(),
    commands: z.array(z.looseObject({})),
  }),
);

/** `agent.queued-commands@1`, as the Rust registry holds it. */
export const AgentQueuedCommandsV1 = registeredMessage(
  "agent.queued-commands@1",
  AgentQueuedCommandsV1Schema,
  {
    type: "object",
    properties: {
      id: {
        type: "integer",
        format: "uint64",
        minimum: 0,
        description: "The number of envelopes before it.",
      },
      author: { $ref: "#/$defs/Author", description: "Who submitted it.", default: "planner" },
      commands: {
        type: "array",
        items: { type: "object", additionalProperties: true },
        description: "The commands, each an object naming its `op`.",
      },
    },
    required: ["id", "commands"],
    description: "One command envelope as `commands.jsonl` holds it.",
    title: "QueuedCommands",
    $schema: "https://json-schema.org/draft/2020-12/schema",
    $defs: { Author: { type: "string", description: "Who wrote a record: an open word." } },
  },
);
