// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `just node-sdk-generate`.
import { z } from "zod";
import { anyOf, contract, oneOf } from "../runtime.js";
import { registeredMessage } from "../../message.js";

/**
 * One request after the hello, discriminated by `op`: one per [`Transport`]
 * method, with `exclusive` split into its opening and its end.
 *
 * Records and documents travel as text, so a plugin carries UTF-8 records; a
 * record that is not UTF-8 is refused by the client before it is sent.
 */
export type OnemessagebusTransportRequestV1 =
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * The record.
       */
      record: string;
      op: "append";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * Read after this position; from the start when absent.
       */
      from?: Position | null | undefined;
      /**
       * At most this many records.
       */
      limit: number;
      op: "read";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * The consumer.
       */
      consumer: string;
      op: "cursor";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * The consumer.
       */
      consumer: string;
      /**
       * Where in a queue a record ends: opaque to consumers, and meaningful only to
       * the transport that handed it out.
       *
       * A consumer receives positions from [`Transport::append`] and
       * [`Transport::read`] and hands them back; it never builds one from a number
       * it made up. It serializes as its token so a cursor survives a process.
       * [`from_token`](Self::from_token) and [`token`](Self::token) exist for a
       * transport's own implementation: [`LocalTransport`]'s token is the byte offset
       * at a record boundary, [`MemoryTransport`]'s the count of records before it.
       */
      at: number;
      op: "commit";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      op: "begin_exclusive";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * Whether the body failed, so the section ends with its failure.
       */
      failed: boolean;
      op: "end_exclusive";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      op: "fingerprint";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      since: Fingerprint;
      /**
       * How long to wait, in milliseconds.
       */
      timeout_ms: number;
      op: "wait_for_change";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * The document.
       */
      name: string;
      op: "document";
    }
  | {
      /**
       * The queue.
       */
      queue: string;
      /**
       * The document.
       */
      name: string;
      /**
       * Its new contents.
       */
      bytes: string;
      op: "replace_document";
    };
/**
 * Where in a queue a record ends: opaque to consumers, and meaningful only to
 * the transport that handed it out.
 *
 * A consumer receives positions from [`Transport::append`] and
 * [`Transport::read`] and hands them back; it never builds one from a number
 * it made up. It serializes as its token so a cursor survives a process.
 * [`from_token`](Self::from_token) and [`token`](Self::token) exist for a
 * transport's own implementation: [`LocalTransport`]'s token is the byte offset
 * at a record boundary, [`MemoryTransport`]'s the count of records before it.
 */
export type Position = number;
/**
 * The fingerprint to wait for it to move from.
 */
export type Fingerprint = number[];

const $QueueName: z.ZodType = z.string();

const $Position: z.ZodType = z.int().gte(0);

const $ConsumerName: z.ZodType = z.string();

const $Fingerprint: z.ZodType = z.array(z.int().gte(0));

const $DocumentName: z.ZodType = z.string();

export const OnemessagebusTransportRequestV1Schema = contract<OnemessagebusTransportRequestV1>(
  oneOf([
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      record: z.string(),
      op: z.literal("append"),
    }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      from: anyOf([z.lazy(() => $Position), z.null()]).optional(),
      limit: z.int().gte(0),
      op: z.literal("read"),
    }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      consumer: z.lazy(() => $ConsumerName),
      op: z.literal("cursor"),
    }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      consumer: z.lazy(() => $ConsumerName),
      at: z.lazy(() => $Position),
      op: z.literal("commit"),
    }),
    z.strictObject({ queue: z.lazy(() => $QueueName), op: z.literal("begin_exclusive") }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      failed: z.boolean(),
      op: z.literal("end_exclusive"),
    }),
    z.strictObject({ queue: z.lazy(() => $QueueName), op: z.literal("fingerprint") }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      since: z.lazy(() => $Fingerprint),
      timeout_ms: z.int().gte(0),
      op: z.literal("wait_for_change"),
    }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      name: z.lazy(() => $DocumentName),
      op: z.literal("document"),
    }),
    z.strictObject({
      queue: z.lazy(() => $QueueName),
      name: z.lazy(() => $DocumentName),
      bytes: z.string(),
      op: z.literal("replace_document"),
    }),
  ]),
);

/** `onemessagebus.transport-request@1`, as the Rust registry holds it. */
export const OnemessagebusTransportRequestV1 = registeredMessage(
  "onemessagebus.transport-request@1",
  OnemessagebusTransportRequestV1Schema,
  {
    oneOf: [
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          record: { type: "string", description: "The record." },
          op: { type: "string", const: "append" },
        },
        required: ["op", "queue", "record"],
        description: "[`Transport::append`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          from: {
            anyOf: [{ $ref: "#/$defs/Position" }, { type: "null" }],
            description: "Read after this position; from the start when absent.",
          },
          limit: {
            type: "integer",
            format: "uint64",
            minimum: 0,
            description: "At most this many records.",
          },
          op: { type: "string", const: "read" },
        },
        required: ["op", "queue", "limit"],
        description: "[`Transport::read`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          consumer: { $ref: "#/$defs/ConsumerName", description: "The consumer." },
          op: { type: "string", const: "cursor" },
        },
        required: ["op", "queue", "consumer"],
        description: "[`Transport::cursor`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          consumer: { $ref: "#/$defs/ConsumerName", description: "The consumer." },
          at: { $ref: "#/$defs/Position", description: "Where it has read up to." },
          op: { type: "string", const: "commit" },
        },
        required: ["op", "queue", "consumer", "at"],
        description: "[`Transport::commit`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          op: { type: "string", const: "begin_exclusive" },
        },
        required: ["op", "queue"],
        description: "Open [`Transport::exclusive`]'s section over a queue.",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          failed: {
            type: "boolean",
            description: "Whether the body failed, so the section ends with its failure.",
          },
          op: { type: "string", const: "end_exclusive" },
        },
        required: ["op", "queue", "failed"],
        description: "End the section the matching `begin_exclusive` opened.",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          op: { type: "string", const: "fingerprint" },
        },
        required: ["op", "queue"],
        description: "[`Transport::fingerprint`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          since: {
            $ref: "#/$defs/Fingerprint",
            description: "The fingerprint to wait for it to move from.",
          },
          timeout_ms: {
            type: "integer",
            format: "uint64",
            minimum: 0,
            description: "How long to wait, in milliseconds.",
          },
          op: { type: "string", const: "wait_for_change" },
        },
        required: ["op", "queue", "since", "timeout_ms"],
        description: "[`Transport::wait_for_change`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          name: { $ref: "#/$defs/DocumentName", description: "The document." },
          op: { type: "string", const: "document" },
        },
        required: ["op", "queue", "name"],
        description: "[`Transport::document`].",
      },
      {
        type: "object",
        additionalProperties: false,
        properties: {
          queue: { $ref: "#/$defs/QueueName", description: "The queue." },
          name: { $ref: "#/$defs/DocumentName", description: "The document." },
          bytes: { type: "string", description: "Its new contents." },
          op: { type: "string", const: "replace_document" },
        },
        required: ["op", "queue", "name", "bytes"],
        description: "[`Transport::replace_document`].",
      },
    ],
    description:
      "One request after the hello, discriminated by `op`: one per [`Transport`]\nmethod, with `exclusive` split into its opening and its end.\n\nRecords and documents travel as text, so a plugin carries UTF-8 records; a\nrecord that is not UTF-8 is refused by the client before it is sent.",
    title: "PluginRequest",
    $schema: "https://json-schema.org/draft/2020-12/schema",
    $defs: {
      QueueName: {
        type: "string",
        description:
          "A queue's name: a non-empty run of ASCII letters, digits, `-` and `_`,\nstarting with a letter or digit — `surfaces`, `command-outcomes`.",
      },
      Position: {
        type: "integer",
        format: "uint64",
        minimum: 0,
        description:
          "Where in a queue a record ends: opaque to consumers, and meaningful only to\nthe transport that handed it out.\n\nA consumer receives positions from [`Transport::append`] and\n[`Transport::read`] and hands them back; it never builds one from a number\nit made up. It serializes as its token so a cursor survives a process.\n[`from_token`](Self::from_token) and [`token`](Self::token) exist for a\ntransport's own implementation: [`LocalTransport`]'s token is the byte offset\nat a record boundary, [`MemoryTransport`]'s the count of records before it.",
      },
      ConsumerName: {
        type: "string",
        description:
          "A consumer's name, held to the same rule as a queue's. The consumer named\n[`ConsumerName::DEFAULT`] is the one a queue with a single reader has.",
      },
      Fingerprint: {
        type: "array",
        items: { type: "integer", format: "uint64", minimum: 0 },
        description:
          "A cheap change token for a queue: two fingerprints of one queue are equal\nwhen nothing observable about it moved.\n\nOpaque, like [`Position`]: a transport builds one from whatever it can\nobserve cheaply ([`LocalTransport`] from each file's length and modification\ntime, as `onepipeline` does), and a consumer only compares them.",
      },
      DocumentName: {
        type: "string",
        description:
          "A document's name: ASCII letters, digits, `-`, `_` and `.`, starting with\na letter or digit — `queue.json`. A name a queue's own files are kept\nunder locally (one ending `.jsonl`, `.torn`, `.staging` or `.lock`, or\nholding `-cursor.`) is refused, so a document never overwrites a queue.",
      },
    },
  },
);
