// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `just node-sdk-generate`.
import { z } from "zod";
import { anyOf, contract, oneOf } from "../runtime.js";

export type SchemaList = SchemaEntry[];

/**
 * One entry of `schema list`.
 *
 * `family` and `version` restate `id` for a reader that does not parse ids, so
 * an entry whose restatement contradicts its id is refused on read rather than
 * handed back with two answers to the same question.
 */
export interface SchemaEntry {
  /**
   * The full id.
   */
  id: string;
  /**
   * The id without its version.
   */
  family: string;
  /**
   * The version.
   */
  version: number;
}

const $SchemaEntry: z.ZodType = z.strictObject({
  id: z.lazy(() => $SchemaId),
  family: z.string(),
  version: z.int().gte(0),
});

const $SchemaId: z.ZodType = z
  .string()
  .regex(new RegExp("^[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+(\\.[A-Za-z0-9_-]+)*@[1-9][0-9]*$", "u"));

export const SchemaListSchema = contract<SchemaList>(z.array(z.lazy(() => $SchemaEntry)));
