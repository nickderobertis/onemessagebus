// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `just node-sdk-generate`.
import { z } from "zod";
import { anyOf, contract, oneOf } from "../runtime.js";

/**
 * What `ask` answered. Every answer names itself in `answer`, and only a
 * reply carries a `reply`: a caller reading the reply member of a timeout, an
 * abandoned listener or a refusal reads nothing.
 */
export type Asked =
  | {
      /**
       * The question's correlation.
       */
      correlation: string;
      /**
       * The reply record, as the answer queue holds it.
       */
      reply: unknown;
      answer: "reply";
      [k: string]: unknown;
    }
  | {
      /**
       * The question's correlation.
       */
      correlation: string;
      answer: "timeout";
      [k: string]: unknown;
    }
  | {
      /**
       * The question's correlation.
       */
      correlation: string;
      answer: "abandoned";
      [k: string]: unknown;
    }
  | {
      /**
       * The question's correlation, when the question was asked.
       */
      correlation?: Correlation | null | undefined;
      /**
       * Why.
       */
      reason: string;
      answer: "refused";
      [k: string]: unknown;
    };
/**
 * Which question a reply answers: the token the bus minted when it was asked.
 */
export type Correlation = string;

const $Correlation: z.ZodType = z
  .string()
  .regex(new RegExp("^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$", "u"));

export const AskedSchema = contract<Asked>(
  oneOf([
    z.looseObject({
      correlation: z.lazy(() => $Correlation),
      reply: z.unknown().refine((value) => value !== undefined, { message: "required" }),
      answer: z.literal("reply"),
    }),
    z.looseObject({ correlation: z.lazy(() => $Correlation), answer: z.literal("timeout") }),
    z.looseObject({ correlation: z.lazy(() => $Correlation), answer: z.literal("abandoned") }),
    z.looseObject({
      correlation: anyOf([z.lazy(() => $Correlation), z.null()]).optional(),
      reason: z.string(),
      answer: z.literal("refused"),
    }),
  ]),
);
