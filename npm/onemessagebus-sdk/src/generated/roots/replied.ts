// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `just sdk-generate`.
import { z } from "zod";
import { anyOf, contract, oneOf } from "../runtime.js";

/**
 * Which question a reply answers: the token the bus minted when it was asked.
 */
export type Correlation = string;

/**
 * What `reply` did: the pending record it answered, and the records it
 * appended — a reply routed by its halves appends more than one, and one with
 * no verdict answers nothing.
 */
export interface Replied {
  /**
   * The pending record answered, or `null` when the reply carried nothing
   * for its answer queue.
   */
  answered?: ClaimedRecord | null | undefined;
  /**
   * The correlation of the ask the reply bound to, when it carries one.
   */
  correlation?: Correlation | null | undefined;
  /**
   * Every record appended, in order.
   */
  sent: Sent[];
}
/**
 * The record `next` claimed.
 */
export interface ClaimedRecord {
  /**
   * The queue it was claimed from.
   */
  queue: string;
  /**
   * Where the claim was recorded: what `reply` names the record by.
   */
  position: number;
  /**
   * The record's id, on a queue that gives one.
   */
  id?: number | null | undefined;
  /**
   * The record.
   */
  record: unknown;
}
/**
 * One record `send` or `reply` appended.
 */
export interface Sent {
  /**
   * The queue it landed on.
   */
  queue: string;
  /**
   * The position after it.
   */
  position: number;
  /**
   * The id it was given, on a queue that gives one.
   */
  id?: number | null | undefined;
}

const $ClaimedRecord: z.ZodType = z.strictObject({
  queue: z.lazy(() => $QueueName),
  position: z.lazy(() => $Position),
  id: anyOf([z.int().gte(0), z.null()]).optional(),
  record: z.unknown().refine((value) => value !== undefined, { message: "required" }),
});

const $QueueName: z.ZodType = z.string();

const $Position: z.ZodType = z.int().gte(0);

const $Correlation: z.ZodType = z
  .string()
  .regex(new RegExp("^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$", "u"));

const $Sent: z.ZodType = z.strictObject({
  queue: z.lazy(() => $QueueName),
  position: z.lazy(() => $Position),
  id: anyOf([z.int().gte(0), z.null()]).optional(),
});

export const RepliedSchema = contract<Replied>(
  z.strictObject({
    answered: anyOf([z.lazy(() => $ClaimedRecord), z.null()]).optional(),
    correlation: anyOf([z.lazy(() => $Correlation), z.null()]).optional(),
    sent: z.array(z.lazy(() => $Sent)),
  }),
);
