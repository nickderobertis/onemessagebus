// Generated by scripts/generate.mjs from the onemessagebus schema bundle. Do not edit; run `just node-sdk-generate`.
import { z } from "zod";
import { anyOf, contract, oneOf } from "../runtime.js";

/**
 * A schema bundle's location — an https:// URL, an http:// URL on a loopback host, a file:// URL, or a path relative to the configuration's directory — and, after a final @, the version pin it is held to: https://example.org/frames.json@8.
 */
export type SchemaLink = string;
/**
 * How a reading verb renders what it read.
 */
export type Format = "json" | "text";

/**
 * The options of `schemas fetch`.
 */
export interface SchemasFetchOptions {
  /**
   * The links to resolve; every link the configuration names when absent.
   */
  links?: SchemaLink[] | null | undefined;
  /**
   * The configuration whose links are resolved when none is named.
   */
  config?: string | null | undefined;
  /**
   * How the report is rendered.
   */
  format?: Format | null | undefined;
}

const $SchemaLink: z.ZodType = z
  .string()
  .refine((value) => [...value].length >= 1, { message: "shorter than 1 characters" });

const $Format: z.ZodType = oneOf([z.literal("json"), z.literal("text")]);

export const SchemasFetchOptionsSchema = contract<SchemasFetchOptions>(
  z.strictObject({
    links: anyOf([z.array(z.lazy(() => $SchemaLink)), z.null()]).optional(),
    config: anyOf([z.string(), z.null()]).optional(),
    format: anyOf([z.lazy(() => $Format), z.null()]).optional(),
  }),
);
